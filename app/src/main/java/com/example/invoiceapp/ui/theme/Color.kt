package com.example.invoiceapp.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Defining the colors as per the provided scheme
val Background = Color(0xFFFFFFFF) // White background
val OnBackground = Color(0xFF1C1B1F) // Text color on the background

// Primary color with different opacities
val Primary100 = Color(0xFF3374F3) // Blue color
val Primary80 = Color(0xCC3374F3) // 80% opacity of Primary
val Primary60 = Color(0x993374F3) // 60% opacity of Primary
val Primary40 = Color(0x663374F3) // 40% opacity of Primary
val Primary20 = Color(0x333374F3) // 20% opacity of Primary

// Secondary color with different opacities
val Secondary100 = Color(0xFFD2E8FD) // Lighter blue color
val Secondary80 = Color(0xCCD2E8FD) // 80% opacity of Secondary
val Secondary60 = Color(0x99D2E8FD) // 60% opacity of Secondary
val Secondary40 = Color(0x66D2E8FD) // 40% opacity of Secondary
val Secondary20 = Color(0x33D2E8FD) // 20% opacity of Secondary

// Confirm colors
val ConfirmRed = Color(0xFFFF7E76) // Red color for errors or warnings
val ConfirmGreen = Color(0xFF3DCA54) // Green color for success confirmations

// Font colors
val FontOnBackground = Color(0xFF00376F) // Text color on the background
val SubText100 = Color(0xFFA8A8A8) // Subtext color with 100% opacity
val SubText50 = Color(0x80A8A8A8) // Subtext color with 50% opacity

// Icon colors
val IconOnBackground = Color(0xFFCEDBF5) // Icon color on the background
val IconDisable = Color(0xFFBDBDBD) // Disabled icon color

// Surface colors
val SurfacePrimary = Color(0xFFECF6FF) // Primary surface color
val SurfaceSecondary = Color(0xFFF5F9FD) // Secondary surface color
val SurfaceDisable = Color(0xFFDDE6EE) // Disabled surface color

// onSurface colors
val OnSurfacePrimary = Color(0xFF3D6D9C) // Primary text color on surfaces
val OnSurfaceSecondary = Color(0xFFA5C7E9) // Secondary text color on surfaces

// Creating the light color scheme
/*val LightColorScheme = ColorScheme(
    primary = Primary40,
    onPrimary = Color.White,
    primaryContainer = Primary100,
    onPrimaryContainer = Color.White,
    secondary = Secondary40,
    onSecondary = Color.White,
    secondaryContainer = Secondary100,
    onSecondaryContainer = Color.White,
    tertiary = ConfirmRed,
    onTertiary = Color.White,
    tertiaryContainer = ConfirmGreen,
    onTertiaryContainer = Color.White,
    error = ConfirmRed,
    errorContainer = ConfirmRed,
    onError = Color.White,
    onErrorContainer = Color.White,
    background = Background,
    onBackground = OnBackground,
    surface = SurfacePrimary,
    onSurface = OnSurfacePrimary,
    surfaceVariant = SurfaceSecondary,
    onSurfaceVariant = OnSurfaceSecondary,
    outline = SubText100,
    inverseOnSurface = FontOnBackground,
    inverseSurface = OnBackground,
    inversePrimary = Primary60,
    shadow = SubText50
    // Additional colors as needed
)*/

// Use LightColorScheme in your theme